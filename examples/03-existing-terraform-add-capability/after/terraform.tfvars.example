# Copy to terraform.tfvars and set values for local runs.
# Required: project, environment. Optional: aws_region, repository.

project     = "my-app"
environment = "dev"
aws_region  = "eu-west-2"
# repository = "https://github.com/org/repo"
