// Placeholder: replace with your app entrypoint.
// When using apigw-lambda-rds, the Lambda handler lives in the blueprint at blueprints/aws/apigw-lambda-rds/src/api/
// When using alb-ecs-fargate-rds, you provide your own Docker image for the ECS service.

console.log('Replace this with your application code.');
