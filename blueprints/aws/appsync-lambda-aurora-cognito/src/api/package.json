{
  "name": "appsync-lambda-resolver",
  "version": "1.0.0",
  "description": "AppSync GraphQL resolver handler for Aurora Serverless v2",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.0.0",
    "@aws-sdk/rds-signer": "^3.0.0",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/pg": "^8.10.0",
    "typescript": "^5.0.0"
  }
}
