node_modules
dist
*.log
.git
.gitignore
*.test.ts
__tests__
.env*
# Exclude other markdown files but keep AGENTS.md
*.md
!AGENTS.md
# Exclude test directories
tests/
scripts/
# Exclude other top-level files
*.sh
*.toml
*.example
# Exclude terraform state and providers (too large for Docker image)
**/.terraform/
**/.terraform.lock.hcl
**/terraform.tfstate
**/terraform.tfstate.backup
**/*.tfstate
**/*.tfstate.backup
# Note: mcp-server source files (src/, tsconfig.json) are needed for Docker build