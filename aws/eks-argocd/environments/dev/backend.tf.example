# environments/dev/backend.tf.example

terraform {
  backend "s3" {
    bucket         = "your-terraform-state-bucket"
    key            = "example-eks-argocd/dev/terraform.tfstate"
    region         = "us-east-1"
    dynamodb_table = "terraform-locks"
    encrypt        = true
  }
}
